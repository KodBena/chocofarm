# Memorandum of Transference: The Chocofarm Undertaking (15 June 2026)

Prepared for the succeeding investigator. It is strictly required to peruse this document in its entirety, alongside the two treatises named under "Preparatory Reading." To cite a document one has merely skimmed constitutes a silent failure of duty (refer to the ADR-0002 registry) — you must read them fully.

## Executive Synopsis

* **Nature of the Work:** The *chocofarm* apparatus is an exercise in Operations Research — specifically, the farming of 'gil' in FFXIII, modelled formally as an *adaptive stochastic orienteering problem* or a *belief-state Markov Decision Process (MDP)*. The measure of success is the quality of the solver and the honesty of our mathematical proofs. It is strictly in the Public Domain and is by no means a mere tool for gaming.
* **The Guiding Principle:** The chief mechanism for surpassing the stubborn rate plateau of ~0.10 is the **calibration of the value-function** (that is, driving the Bellman residual of the belief state-value towards naught), rather than expanding network capacity or search width. The present session has yielded three independent but converging lines of evidence to support this postulate.
* **Immediate Obligations:** One must first re-verify and merge the JAX-training apparatus (which presently rests upon a single linear branch; see "Condition of the Repository"). Thereafter, the true labour lies in pursuing the calibration propositions outlined in `consult-003`.

## Required Preparatory Reading

You are instructed to read the following in full, in the order presented:

1. This present memorandum.
2. `docs/consults/consult-003-marry-static-az-katago.md` — The strategic course (enumerating our ranked propositions and an honest verdict). This forms the backbone of your forthcoming endeavours.
3. `docs/design/dual-bound.md` — The exposition of the provable ceiling, demonstrating why a calibrated state-value remains our most vital lever.

**Supplementary texts, to be consulted as the work demands:** `docs/results/voi-ceiling-2026-06-13.md` (concerning the 0.1454 ceiling limit), `docs/results/decomp-rate.md` (identifying the locus of the mathematical gap), `docs/results/az-residual.md` alongside `az-edecide.md` (establishing value as a geometry-blind progress indicator), and `docs/notes/jaxtrain-deadlock-rca.md` (concerning the root cause analysis of the deadlock on the repair branch).

## The Frontier of Rates (Numerical Observations)

| Regimen | Rate | %VoI | Remarks |
| --- | --- | --- | --- |
| Static Floor | 0.0855 | 0% | Fixed route; wholly devoid of sensing |
| Exact Decomposition | 0.094 | +14% | The first instance to rise above the floor |
| **AlphaZero Best** | **~0.097–0.10** | **+20–27%** | The first to surpass decomposition |
| Clairvoyant Ceiling | 0.1454 | 100% | **Loose bound**; extracts no penalty for sensing |

The percentage of Value of Information (%VoI) is derived thusly:

$$ \text{%VoI} = \frac{\text{rate} - 0.0855}{0.1454 - 0.0855} $$

The clairvoyant ceiling remains loose, as the true difficulty (the VoI secured behind deep, contingent chains of sensing) represents the very cost it circumvents. The dual bound imposes a strict charge for this; its value for the complete instance is yet pending (refer to "Pending Endeavours", Item 5).

During this session, a matched experimental trial (Residual ON versus OFF, $m=24$, JAX-trained) revealed that **both approach a plateau at ~0.10**. The capacity of the residual block does not alter the rate — a finding entirely consistent with our maxim: "calibration, not capacity." The empirical data has been preserved within `runs/matched_resoff` and `runs/matched_reson`.

## Condition of the Repository — The JAX-Training Stack (Imperative)

The primary trunk (`main`, presently at `2cbbbd8` and pushed to the origin) incorporates the residual block, the machinery for the **dual bound**, and **consult-003**. However, the JAX-training apparatus resides upon a **linear succession of branches not yet united with the main trunk**:

```
main 2cbbbd8 ─ residual + dual-bound + consult-003   (pushed)
feat/az-jax-train  e283978  ── JAX/optax training migration
  └ fix/jaxtrain-deadlock  0654bf7 (deadlock fix) + 17d835d (--resume flag)
     └ cleanup/remove-numpy-adam  09e6bcf (hand-rolled-Adam removal)  ← TIP

```

The active worktrees are thus demarcated: `chocobo` (main), `chocobo-jaxtrain` (feat/az-jax-train), `chocobo-jaxfix` (fix/jaxtrain-deadlock), and `chocobo-cleanup` (cleanup/remove-numpy-adam).

The tip of this stack is `cleanup/remove-numpy-adam`, which **contains the entirety of the modifications** (the JAX migration, the deadlock resolution, the resume flag, and the Adam removal). To merge this tip into `main` is to adopt the whole stack. As `main` has diverged via the dual-bound and consult additions, a three-way merge is required. The file paths are largely distinct (`chocofarm/az/*` against `chocofarm/bounds/*` and `docs/*`), so it ought to prove a clean operation. Nevertheless, prudence dictates you verify with `git merge-tree` or a dry rehearsal prior to execution.

## Accomplishments of the Present Session

* **Merged and pushed to the main trunk:** The dual-bound machinery and consult-003 (following the earlier inclusion of the residual block and the firewall consult).
* **The Dual Bound** (`docs/design/dual-bound.md`): Confirmed valid by means of information-relaxation duality (per Brown–Smith–Sun). It holds universally that:
$$ \rho^* \le \min(\bar{\lambda}, 0.1454) $$
This has been validated upon sub-instances (demonstrating strong duality when $\hat{V} = V^*$). **Calculation of the full-instance figure is presently deferred**. The flat inner solver proves intractable upon the complete belief state; it demands a decomposition-aligned *separable* inner solver (as specified in Section 4.4) **coupled with** a calibrated state-value $\hat{V}$ (the decomposition decision-value is too loose; a trained AlphaZero value network serves as the tractable, calibrated generator).
* **Consult-003:** We have ranked the propositions for calibration. The verdict dictates that this is worth pursuing *narrowly* (as rate gains are modest, and the gap is structurally bound by the reward frontier). Yet, labour spent on value-calibration promises a guaranteed dividend: it tightens the certified ceiling whilst simultaneously improving the policy—artifacts which, by strong duality, are one and the same.
* **Resolution of the Deadlock** (`fix/jaxtrain-deadlock`): The parallel loop under JAX training suffered intermittent deadlocks (the parent process pausing in `multiprocessing` `imap_unordered`, awaiting a worker incapacitated by what was most likely thread-environment residue from JAX-to-spawn-child; the audit and analysis reside in `docs/notes/jaxtrain-deadlock-rca.md`). The remedy involves bounded draining of the imap results per iteration — yielding a **loud RuntimeError in lieu of an infinite hang** (ADR-0002) — alongside redis socket timeouts, thread-environment pins for workers, faulthandlers, a time-to-live for result-blobs, and bounded teardown. **Validation:** The amended code navigated cleanly *through* the iterations (27 to 29) where the previous apparatus faltered; six distinct deadlock tests now pass.
* **The `--resume` Flag** (`17d835d`): This instrument loads the ENTIRE network (trunk, residual, value, and policy) to recommence training. It has been verified comprehensively that the resumed residual block (`Wr1`) and policy head (`Wp`) securely inhabit the optax training parameters. This is distinct from `--init-weights` (a legacy of E-DECIDE which leaves policy and residual randomised).
* **Removal of Hand-Rolled Adam** (`cleanup/remove-numpy-adam`, `09e6bcf`): We have excised the obsolete numpy Adam and the manual steps (`train_step`, `_residual_backward`, `_adam_apply`) from `mlp.py` (which is now strictly for inference), as well as lingering `_init_adam()` invocations. The agent reports **32 tests passing** alongside an external audit. **Take heed: I have not yet independently re-verified this, as our session was interrupted before the suite could be re-run. You must execute the test suite prior to merging.**

## Pending Endeavours (In Order of Priority)

1. **Re-verify and merge the stack.** From within the `chocobo-cleanup` directory, execute the following directive:
```bash
PYTHONPATH=. timeout 600 taskset -c 3 <py> -m pytest tests/test_az_loop.py tests/test_jax_equivalence.py tests/test_parallel_deadlock.py -q

```



```
    Anticipate 32 passing tests. The **jax-equivalence test acts as the principal safeguard**, ensuring that numpy inference continues to match the compiled forward pass. Upon success, merge `cleanup/remove-numpy-adam` into `main`. Note that pushing to origin requires the consent of the human maintainer (an automated guardrail prevents pushing session-born branches without explicit authorisation, a snare which has caught us before).
2.  **Amend the `train_value.py` docstring** (circa lines 6–7): It currently mentions "manual-Adam SGD" despite employing `JaxTrainer`. This is an antiquated vestige which the maintainer has ordered struck, noting it causes confusion amongst LLM collaborators. Incorporate this correction into the cleanup branch before merging. (This was the very task in progress when our session concluded).
3.  **The Learning-Rate Anneal Experiment — QUEUED, yet to be launched.** The maintainer has requested that we *resume* (not restart) from the converged network in `matched_reson`, employing an LR of `1e-4` (0.1×) and an integer of $I=80$ (2×), to ascertain if a finer rate coaxes progress past ~0.10. Conduct this upon the CLEAN code following the merge. The directive is thus:
    ```bash
    PYTHONPATH=<worktree> PYTHONFAULTHANDLER=1 <py> -m chocofarm.az.exit_loop \
      -I 80 -E 300 -W 5 --epochs 2 --batch 256 --m 24 --n-sims 128 --lr 1e-4 --l2 1e-4 \
      --alpha 1.0 --beta 2.0 --lam 0.0855 --explore-plies 4 --eval-n 200 --eval-seed 12345 \
      --seed 7 --td-lambda 0.6 --workers 4 --cores 0,1,2,3 --hidden 256 --residual \
      --resume runs/matched_reson/latest_net.npz \
      --tb-logdir tb/az/lr1e4_resume --ckpt-dir runs/lr1e4_resume

```

```
This is purely exploratory — the maintainer has noted that this is "not really the point we're at."

```

4. **The True Course — Consult-003's Three Calibration Propositions** (This is the crux of the matter):
* (1) A Bellman-consistency value target (a TD(0) auxiliary loss utilising the closed-form belief outcome expectation, dispensing with rollouts).
* (2) An exact-micro-value calibrated label upon in-cluster beliefs (repurposing `enter_value` from `decomp.py`).
* (3) A per-cluster occupancy-$k$ auxiliary head.
*The metric of success for the first proposition completes the circuit with the dual bound:* freeze the trained network, supply $\hat{V}_{AZ}$ into `chocofarm/bounds/eval_bound.py`, and observe whether $\bar{\lambda}$ falls beneath 0.1454.


5. **Derivation of the Dual-Bound Full-Instance Figure** (contingent upon the existence of a calibrated AlphaZero value network): Construct the separable inner solver (per `dual-bound.md`, Section 4.4), and employ the frozen network as $\hat{V}$. This shall reveal the provable, genuine ceiling above the ~0.10 threshold.

## Operational Particulars

* **Python Interpreter:** `/home/bork/w/vdc/venvs/generic/bin/python` (furnished with JAX, optax, and numba). This serves as the communal scratch environment from which the script operates.
* **Redis Scratchpad:** `127.0.0.1:6380` (utilised for worker transport via raw bytes).
* **Experimental Records:** Kept within `~/w/vdc/chocobo/runs/` (housing checkpoints and `.log` files). The TensorBoard apparatus resides at `~/w/vdc/chocobo/tb/az/` (the daemon serves `--logdir tb/az` upon port `6006`). Both directories are deliberately excluded from version control. **Never discard the fruits of your experiments; preserve them under `~/w/vdc`, eschewing `/tmp`.**
* **Host Apparatus:** A 4-vCPU libvirt virtual machine; execution must be pinned via `--cores 0,1,2,3`, bearing a parallel ceiling of approximately 1.9×.
* **Diagnostic Constraints:** Due to `ptrace_scope=1`, the utility `py-spy` **cannot** attach to a process already in motion. Should an execution hang, you must either launch it with `PYTHONFAULTHANDLER=1` and issue `kill -ABRT` (which shall decant all thread tracebacks to standard error or the logs), or commence the process directly *beneath* `py-spy`.
* **Dormant Daemons:** The paused classical-baseline processes (`nmcs_l2`, `nmcs_l3`, `ismcts`) are halted writers possessing directories within `tb/` — do not disturb or slay them unless they are definitively declared stale.
* **Model Hierarchy:** Be advised that "Fable" is **unavailable** within this environment (attempts to launch it will fail). Direct all matters of design and verification to the "Opus" tier.

## Disposition at the Close of the Session

* **No Active AlphaZero Operations:** The cold restart at LR `1e-4` was arrested, and the matched trials are concluded and secured. Ascertain this by issuing `pgrep -f chocofarm.az.exit_loop` prior to commencing new tasks.
* The re-verification of the Adam cleanup and the sweep of the `train_value.py` docstring were the twin tasks occupying my attention when the session met its end. I counsel you to commence your labours there (refer to Pending Endeavours, Items 1 and 2).
