"""
tools/analysis/leaf_eval_bound/contract/__init__.py

the typed contract layer: `estimate` (the Estimate SSOT) + `grounding` (the Band-3 grounded constants). Imports nothing from the package above the stdlib/numpy contract.

Public Domain (The Unlicense).
"""
