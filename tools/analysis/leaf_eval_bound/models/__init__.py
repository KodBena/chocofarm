"""
tools/analysis/leaf_eval_bound/models/__init__.py

the things-being-bounded: the static-grounded models + the manifest-driven transport variants, each declaring one JAX-traceable `throughput_jax` the `alloc` driver consumes.

Public Domain (The Unlicense).
"""
