"""
tools/analysis/leaf_eval_bound/runners/__init__.py

the entry-point programs (throughput_bound / transport_sweep / untrusted_drive). Run standalone via `python -m leaf_eval_bound.runners.<name>` from tools/analysis (or PYTHONPATH=tools/analysis).

Public Domain (The Unlicense).
"""
