"""
tools/analysis/leaf_eval_bound/store/__init__.py

the Postgres egress + registry resolver: `bench_store` (SQL), `manifest` (name->Estimate), `reconstruct` (seed/aggregate->Estimate glue).

Public Domain (The Unlicense).
"""
